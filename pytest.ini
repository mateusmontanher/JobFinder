[pytest]
testpaths = ML/tests webscrapping/tests tests
addopts = -p no:cacheprovider --strict-markers
markers =
    browser: requires a Playwright browser runtime
    performance: local deterministic performance budget
